import 'package:flutter/material.dart';

class DogDetailsComponent extends StatelessWidget {
  const DogDetailsComponent({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
